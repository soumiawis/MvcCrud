{% extends 'base.html.twig' %}

{% block title %} Ajouter un Etudiant{% endblock %}

{% block body   %}
        <div class="container">
             <h1>Créer un nouveau Etudiant</h1>

            {{ form_start(f, {'attr': {'novalidate': 'novalidate'}}) }}
                {{ form_row(f.nom) }}
                {{ form_row(f.niveau, { placeholder : '-- Selectionner une type --',label : 'niveau '}) }}
                {{ form_row(f.email) }}
                {{ form_row(f.tel) }}
                <button type="submit" class="btn btn-primary">Envoyer</button>
            {{ form_end(f) }}

        </div>

{% endblock %}